# Runtime dependencies of the PrintLink tray agent.
# Pinned for reproducible PyInstaller builds — bump deliberately, not casually.
cryptography==50.0.0
Flask==3.1.3
Pillow==12.3.0
pymupdf==1.28.2
pystray==0.19.5
pywin32==312; sys_platform == 'win32'
requests==2.34.2
zeroconf==0.150.0
